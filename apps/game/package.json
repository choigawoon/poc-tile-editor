{
  "name": "@poc/game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "③ The example game runtime. PixiJS (WebGL) renderer; consumes a bundle, imports @poc/core for gid math.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@poc/core": "*",
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
