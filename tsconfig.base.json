{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Incremental TypeScript adoption: type-check JS via JSDoc. Files opt in with `// @ts-check`; checkJs stays false so untyped files don't error yet. Convert file-by-file (add `// @ts-check`, then later rename to .ts).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true
  }
}
